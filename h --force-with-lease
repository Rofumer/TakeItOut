[33m7bdcfa3[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mmaster-1.21.8[m[33m)[m - **Hologram ray tracing**: resolves the target `BlockState` from `SchematicWorld` even if the ray hits air/hologram only. - **Auto item acquisition**:   - Search inventory first; if missing, request from shulker (`GetShulkerStackPayload`).   - Always calls `litematica.util.InventoryUtils.schematicWorldPickBlock(...)` to keep Easy Place consistent. - **Ping-aware waiting**:   - Timeout based on ping (~2.5 RTT + buffer), converted to ticks.   - Two retries with jitter; hard timeout resets waiting state. - **Vanilla safety**:   - If main hand already holds a placeable block, right-click is not intercepted.   - With Easy Place off, right-click on holograms only triggers `pick block`, vanilla click is preserved. - **Bounded waiting**: min/max limits prevent hanging under high latency. - **Mixins**:   - `doEasyPlaceAction` → missing item detection & wait start.   - `easyPlaceOnUseTick` → tick loop, retries, timeout.   - `doSchematicWorldPickBlock` → schematic-space ray trace, inventory/shulker lookup, litematica pickblock call.
